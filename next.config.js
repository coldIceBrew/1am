/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "yt3.googleusercontent.com",
      "picsum.photos",
      "www.buzzsprout.com",
    ],
  },
};

module.exports = nextConfig;
